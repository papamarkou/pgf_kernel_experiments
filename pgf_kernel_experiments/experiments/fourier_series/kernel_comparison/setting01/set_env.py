# %% Import packages

from pathlib import Path

# %% Set paths

data_path = Path('data')
output_path = Path('output')

# %% Set seeds

seed = 4

train_subset1_seed = seed + 1
train_subset2_seed = seed + 2
train_subset3_seed = seed + 3
train_subset4_seed = seed + 4
